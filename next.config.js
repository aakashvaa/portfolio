/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [],
    },
    // Required for twin.macro alternative - we'll use plain Tailwind in Next.js
}

module.exports = nextConfig
